---
// packages/web/src/pages/index.astro
//
// Landing page for sidecoin.app

import BaseLayout from "../layouts/BaseLayout.astro";
import ForkCountdown from "../components/ForkCountdown.vue";
import SidechainCard from "../components/SidechainCard.astro";
import { SIDECHAINS, ACTIVE_SIDECHAIN_COUNT } from "../data/sidechains";
---

<BaseLayout>
  <!-- ─── Hero ──────────────────────────────────────────────── -->
  <header class="relative overflow-hidden">
    <div class="absolute inset-0 bg-gradient-to-b from-ecash-950/20 via-gray-950 to-gray-950"></div>

    <div class="relative mx-auto max-w-5xl px-6 pb-20 pt-24 text-center md:pt-32">
      <h1 class="text-5xl font-extrabold tracking-tight md:text-7xl">
        <!-- Stylized brand mark (visual only) + accessible/SEO text -->
        <span
          class="bg-gradient-to-br from-ecash-400 via-ecash-500 to-ecash-600 bg-clip-text text-transparent"
          aria-hidden="true"
        >SidΞcoin</span>
        <span class="sr-only">Sidecoin</span>
      </h1>
      <p class="mx-auto mt-4 max-w-2xl text-lg text-gray-400 md:text-xl">
        Open-source wallet for the eCash hard fork.<br />
        BIP-300 / BIP-301 Drivechains · {ACTIVE_SIDECHAIN_COUNT} sidechains live at launch.
      </p>

      <div class="mt-12">
        <ForkCountdown client:load />
      </div>

      <div class="mt-12 flex flex-wrap items-center justify-center gap-4">
        <a
          href="https://github.com/APECSdev/sidecoin/releases"
          class="rounded-lg bg-ecash-600 px-8 py-3 text-sm font-semibold text-white no-underline shadow-lg shadow-ecash-600/20 transition-colors hover:bg-ecash-500 hover:no-underline"
        >
          Download Desktop
        </a>
        <a
          href="https://f-droid.org/packages/app.sidecoin.wallet/"
          class="rounded-lg border border-gray-700 bg-gray-900 px-8 py-3 text-sm font-semibold text-gray-300 no-underline transition-colors hover:border-ecash-700 hover:text-white hover:no-underline"
        >
          F-Droid (Android)
        </a>
        <a
          href="https://github.com/APECSdev/sidecoin"
          class="rounded-lg border border-gray-700 bg-gray-900 px-8 py-3 text-sm font-semibold text-gray-300 no-underline transition-colors hover:border-gray-600 hover:text-white hover:no-underline"
        >
          View Source
        </a>
      </div>
    </div>
  </header>

  <!-- ─── What is Sidecoin ──────────────────────────────────── -->
  <section id="downloads" class="mx-auto max-w-5xl px-6 py-20">
    <h2 class="text-center text-3xl font-bold md:text-4xl">
      What is <span class="text-ecash-400" aria-hidden="true">SidΞcoin</span><span class="sr-only">Sidecoin</span>?
    </h2>
    <p class="mx-auto mt-6 max-w-3xl text-center text-lg leading-relaxed text-gray-400">
      Sidecoin is a multi-platform wallet built for the upcoming eCash hard fork
      (block ~964,000, August 21 2026). It supports BIP-300 Hashrate Escrows and
      BIP-301 Blind Merged Mining — enabling trustless sidechains secured by
      Bitcoin's full SHA-256d hashrate.
    </p>

    <div class="mt-16 grid gap-8 md:grid-cols-3">
      <div class="rounded-xl border border-gray-800 bg-gray-900/40 p-6">
        <div class="mb-3 text-3xl">🖥️</div>
        <h3 class="text-lg font-semibold text-white">Desktop</h3>
        <p class="mt-2 text-sm text-gray-400">
          Native app powered by Tauri + Rust. Full node connectivity,
          hardware wallet support, and BIP-300 deposit/withdrawal management.
        </p>
      </div>

      <div class="rounded-xl border border-gray-800 bg-gray-900/40 p-6">
        <div class="mb-3 text-3xl">📱</div>
        <h3 class="text-lg font-semibold text-white">Mobile</h3>
        <p class="mt-2 text-sm text-gray-400">
          React Native app for Android (F-Droid) and iOS. QR scanning,
          biometric auth, and lightweight SPV mode for on-the-go payments.
        </p>
      </div>

      <div class="rounded-xl border border-gray-800 bg-gray-900/40 p-6">
        <div class="mb-3 text-3xl">🌐</div>
        <h3 class="text-lg font-semibold text-white">Web</h3>
        <p class="mt-2 text-sm text-gray-400">
          Browser-based wallet (coming soon). No install required — connect
          to your node and manage sidechains from any device.
        </p>
      </div>
    </div>
  </section>

  <!-- ─── Sidechains ────────────────────────────────────────── -->
  <section id="chains" class="mx-auto max-w-5xl px-6 py-20">
    <h2 class="text-center text-3xl font-bold md:text-4xl">
      {ACTIVE_SIDECHAIN_COUNT} Sidechains at Launch
    </h2>
    <p class="mx-auto mt-4 max-w-2xl text-center text-gray-400">
      Each sidechain occupies a (sparse) BIP-300 slot and is secured by Bitcoin's
      full hashrate via Blind Merged Mining (BIP-301). RISCy is reserved at
      slot 3 and not yet activated.
    </p>

    <div class="mt-12 grid gap-4 md:grid-cols-2">
      {SIDECHAINS.map((sc) => (
        <SidechainCard
          slotNumber={sc.slot}
          name={sc.displayName}
          description={sc.description}
          status={sc.status}
        />
      ))}
    </div>
  </section>

</BaseLayout>
